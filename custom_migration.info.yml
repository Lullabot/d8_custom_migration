name: Custom Migration
type: module
description: Custom migration code to clean up things not handled automatically by core migration.
core: 8.x
dependencies:
  - migrate
  - migrate_drupal
  - migrate_tools
  - migrate_upgrade
  - devel
  - kint

